<!DOCTYPE html>
<html>
  <head>
    <title>Password Reset</title>
    <style>
      .title {
        font-family: "Noto Sans Arabic", sans-serif;
        font-weight: bold;
      }

      .content {
        font-family: "Poppins", sans-serif;
      }

      body {
        font-family: "Noto Sans Arabic", sans-serif;
        padding: 20px;
      }

      .container {
        height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .reset-button {
        background-color: #4caf50;
        /* Green */
        border: none;
        color: white;
        padding: 12px 26px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        border-radius: 4px;
      }

      .reset-button:hover {
        background-color: #45a049;
      }
    </style>
  </head>

  <body>
    <p>Dear <%= name %>,</p>
    <div class="content">
      <p>
        A password reset was requested for your account. Use the link below to
        reset your password. If clicking the link does not work, copy and paste
        it into your browser's address bar.
      </p>

      <p><a href="<%= resetLink %>"> <%= resetLink %> </a></p>

      <p>
        If you did not request a password reset, do not follow the link and
        disregard this email.
      </p>
    </div>
    <p>Sincerely,</p>
    <p>Insight Flow Team</p>
  </body>
</html>
